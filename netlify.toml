[build]
  command = "dotnet publish -c Release -o artifacts/publish && python tools/export_static.py"
  publish = "dist"

[build.environment]
  DOTNET_VERSION = "10.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
